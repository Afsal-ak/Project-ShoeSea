<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Success</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    <style>
        .order-success {
            margin-top: 50px;
            text-align: center;
        }
        .order-summary {
            margin-top: 30px;
        }
        .btn-back-home {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <%- include('../partials/user/header') %>

    <div class="container order-success">
        <div class="alert alert-success" role="alert">
            <h4 class="alert-heading">Order Placed Successfully!</h4>
            <p>Thank you for your purchase, <%= user.username %>! Your order has been placed successfully.</p>
        </div>
        <h5>Order Number: <%= order._id %></h5>
        <p>We will send a confirmation email with the details shortly.</p>

        <!-- Order Summary Section -->
        <div class="order-summary">
            <h3>Order Summary</h3>
            <ul class="list-group">
                <% order.products.forEach(item => { %>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        <%= item.productId.productName %> (x<%= item.quantity %>)
                        <span>$<%= item.price * item.quantity %></span>
                    </li>
                <% }) %>
            </ul>
            <h5 class="mt-3">Total Amount: $<%= order.totalAmount %></h5>
        </div>

        <!-- Back to Home Button -->
        <a href="/" class="btn btn-primary btn-back-home">Back to Home</a>
    </div>

    <%- include('../partials/user/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
