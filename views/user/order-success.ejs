<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Success</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .order-success {
            margin-top: 60px;
            text-align: center;
        }
        .alert {
            background-color: #28a745;
            color: white;
            border-radius: 8px;
            padding: 30px;
        }
        .alert-heading {
            font-size: 1.8rem;
        }
        .order-summary {
            margin-top: 40px;
        }
        .list-group-item {
            background-color: #ffffff;
            border: 1px solid #ddd;
            border-radius: 5px;
            margin-bottom: 10px;
        }
        .total-amount {
            font-size: 1.2rem;
            font-weight: 500;
            color: #495057;
        }
        .btn-back-home {
            margin-top: 30px;
            padding: 12px 30px;
            font-size: 1.1rem;
            background-color: #007bff;
            border-radius: 30px;
        }
        .btn-back-home:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>

    <%- include('../partials/user/header') %>

    <div class="container order-success">
        <div class="alert alert-success shadow-sm">
            <h4 class="alert-heading">Order Placed Successfully!</h4>
            <p>Thank you for your purchase, <strong><%= user.username %></strong>! Your order has been placed successfully.</p>
        </div>

        <div class="order-details mt-4">
            <h5 class="text-muted">Order Number: <strong><%= order.orderId %></strong></h5>
            <p>We will send a confirmation email with the details shortly.</p>
        </div>

        <!-- Order Summary Section -->
        <div class="order-summary">
            <h3 class="text-primary mb-4">Order Summary</h3>
            <ul class="list-group shadow-sm">
                <% order.products.forEach(item => { %>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        <div>
                            <strong><%= item.productId.productName %></strong> <br>
                            Quantity: <%= item.quantity %>
                        </div>
                        <span class="fw-bold">₹<%= (item.price * item.quantity).toFixed(2) %></span>
                    </li>
                <% }) %>
            </ul>
            <h5 class="total-amount mt-4">Total Amount: ₹<strong><%= order.totalAmount.toFixed(2) %></strong></h5>
        </div>

        <!-- Back to Home Button -->
        <a href="/" class="btn btn-primary btn-back-home shadow-sm">Back to Home</a>
    </div>

    <%- include('../partials/user/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
